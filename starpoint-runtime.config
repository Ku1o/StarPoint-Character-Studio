<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <runtime>
    <!-- The workbench ships its managed assemblies locally. Windows may mark
         a downloaded archive as Internet-zone content; keep the bundled
         assemblies loadable without asking artists to unblock each DLL. -->
    <loadFromRemoteSources enabled="true" />
  </runtime>
</configuration>
