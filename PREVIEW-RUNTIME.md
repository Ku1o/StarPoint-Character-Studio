# 技能完整预览的接入审查

0.5.0 的两个入口共用素材播放器、原生 PartsAnimation 解码、确定事件的技能时序解析、音效调度和编译回读。它们尚未包含完整战斗模拟；嵌入 MOD 不会自动补齐目标、碰撞、镜头和条件分支。

## 已核实的实现

原 MOD 的 `wf_gui.py` 中，角色动作支持播放；`effect_previews` 提供特效分帧、时间轴和声音信息，`skill_effect_summary` 提供技能程序摘要。独立的 `wf_flatomo_preview_render.py` 提供原生特效图层合成，角色工坊已复用它。这些功能不是完整的战斗技能播放器。

审查基于 2026-09-12 已接受的 Android 客户端，内嵌 SWF SHA-256 为 `52b32a54553be65e62d781fcdf1dff4e136d1452dfdf03cfe19607a010a76a15`。只读校验并导出指定类，没有启动或修改客户端。

- `GachaSkillPreviewTools.getSkillPreviewValues` 从角色预览表读取演示关卡、回放路径和裁切信息。
- `BattleScene.startPreview` 使用 `SkillPreviewSceneStateImpl`；该状态通过资源容器读取回放配置与指令。
- `SkillPreviewSceneStateImpl.updateState` 把回放指令写入 `scene.myBattle`，并调用 `Battle.proceed()` 推进实际战斗。它不直接播放一组现成图片。
- `run` 设置初始位置、速度、HP、技能槽与强化弹射等级。`SkillPreviewQuestLogic` 则依赖关卡主表、角色数据、任务仓库和资源容器建立场景。

单角色素材包不包含整个场景、敌人、公共反馈资源和运行引擎。0.5 的独立定义包保留 489 个现有官方演示回放及 505 个角色定义；回放存在只能证明指令已收录，不能标记完整演示已实现。

## 继续实现的结构

客户端预览应作为两个入口共用的独立预览服务。引擎和游戏资源包与工具源码分开提供，接收经过校验的工程快照，并在隔离目录中加载覆盖资源。服务应返回所用引擎、表版本、覆盖文件哈希和缺失资源；不能把已有素材预演伪装成客户端回放。

先完成一位官方角色的原生关卡回放，再验证移动/命中型技能、条件技能、多阶段技能、特殊图层和声音。接着将单角色替换资源接入同一个场景，核对编译产物与实际加载文件一致。必要依赖包括离线资源容器、预览关卡与敌人主表、公共图片/音效、运行宿主、场景渲染和输入/窗口生命周期。

上述原生场景播放器在本版仍未实现，也未进行真机或 AIR 战斗验收。界面的成品预览要求继续保留为目标；任何相关限制属于未完成项。
